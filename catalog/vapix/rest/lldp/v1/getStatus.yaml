id: lldp:getStatus
cgi: lldp

method: GET
base_path: /config/rest/lldp/v1
path: /status

response:
  format: json

risk_level: read-only

notes: >
  Stubbed — verify exact path against device API discovery.
  Returns LLDP status including whether the protocol is active.

requires:
  auth_level: viewer

id: cert:removeCertificate
cgi: cert

method: DELETE
base_path: /config/rest/cert/v1
path: /certificates/{alias}

response:
  format: json

risk_level: service-affecting
service_impact: >
  Removing an active certificate may break HTTPS or 802.1X
  connections to the device.

requires:
  auth_level: admin

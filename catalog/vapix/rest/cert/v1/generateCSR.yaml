id: cert:generateCSR
cgi: cert

method: POST
base_path: /config/rest/cert/v1
path: /certificates/{alias}/get_csr

request:
  content_type: application/json

response:
  format: json

risk_level: normal

requires:
  auth_level: admin

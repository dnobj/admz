id: ssh:addUser
cgi: ssh

method: POST
base_path: /config/rest/ssh/v2
path: /users

request:
  content_type: application/json

response:
  format: json

risk_level: normal

requires:
  auth_level: admin

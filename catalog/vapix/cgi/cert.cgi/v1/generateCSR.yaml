id: cert.cgi:generateCSR
cgi: cert.cgi

method: POST
base_path: /config/rest/cert/v1
path: /certificates/{alias}/get_csr

request:
  content_type: application/json

response:
  format: json

risk_level: normal

requires:
  auth_level: admin

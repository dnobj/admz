id: mdnssd.cgi:getMdnssdInfo
cgi: mdnssd.cgi

method: POST
min_api_version: "1.0"

request:
  content_type: application/json
  body:
    apiVersion: "1.0"
    method: getMdnssdInfo

response:
  format: json
  data_path: data
  fields:
    - enabled
    - friendlyName

risk_level: read-only

requires:
  auth_level: admin

id: mediaclip.cgi:stop
cgi: mediaclip.cgi

method: GET
request:
  query:
    action: stop
    audiooutputid: "{audiooutputid}"
  content_type: null

response:
  format: text
  success: "OK"

risk_level: normal

requires:
  auth_level: operator

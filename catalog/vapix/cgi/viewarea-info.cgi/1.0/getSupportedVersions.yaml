id: viewarea-info.cgi:getSupportedVersions
cgi: viewarea-info.cgi

method: POST
min_api_version: "1.0"

request:
  content_type: application/json
  body:
    apiVersion: "1.0"
    method: getSupportedVersions

response:
  format: json
  data_path: data
  fields:
    - apiVersions

risk_level: read-only

requires:
  auth_level: viewer

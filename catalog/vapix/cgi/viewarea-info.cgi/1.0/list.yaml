id: viewarea-info.cgi:list
cgi: viewarea-info.cgi

method: POST
min_api_version: "1.0"

request:
  content_type: application/json
  body:
    apiVersion: "1.0"
    method: list

response:
  format: json
  data_path: data
  fields:
    - viewAreas

risk_level: read-only

requires:
  auth_level: viewer

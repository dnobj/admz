id: time.cgi:get-date
cgi: time.cgi

method: GET
request:
  query: {}
  content_type: null

response:
  format: text
  description: >
    Returns date/time string, e.g. "Jan 15 2024 14:30:00 EST"

risk_level: read-only

requires:
  auth_level: viewer

id: opticscontrol.cgi:setMagnification
cgi: opticscontrol.cgi

method: POST
min_api_version: "1.0"

request:
  content_type: application/json
  body:
    apiVersion: "1.0"
    method: setMagnification
    params:
      optics:
        - opticsId: "{opticsId}"
          magnification: "{magnification:float}"

response:
  format: json
  data_path: data

risk_level: normal

requires:
  auth_level: admin

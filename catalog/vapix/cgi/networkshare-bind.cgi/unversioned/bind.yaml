id: networkshare-bind.cgi:bind
cgi: networkshare-bind.cgi

method: GET
request:
  query:
    schemaversion: "{schemaversion}"
    shareid: "{shareid}"
  content_type: null

response:
  format: text
  description: >
    Binds the network share and returns the assigned disk ID.

risk_level: normal

requires:
  auth_level: admin

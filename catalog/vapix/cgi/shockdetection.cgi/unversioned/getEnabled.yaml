id: shockdetection.cgi:getEnabled
cgi: shockdetection.cgi

method: GET
request:
  query:
    schemaversion: "{schemaversion}"
  content_type: null

response:
  format: text
  description: >
    Returns XML indicating whether shock detection is enabled.

risk_level: read-only

requires:
  auth_level: operator

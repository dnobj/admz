id: log-config.cgi:setPersistentEnabled
cgi: log-config.cgi

method: PATCH
base_path: /config/rest/log/v1alpha
path: /persistent

request:
  content_type: application/json

response:
  format: json

risk_level: normal

requires:
  auth_level: admin

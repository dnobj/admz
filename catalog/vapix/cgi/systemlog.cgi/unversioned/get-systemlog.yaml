id: systemlog.cgi:get-systemlog
cgi: systemlog.cgi

method: GET

request:
  query: {}
  content_type: null

response:
  format: text
  description: >
    Returns device system log entries as plain text.

risk_level: read-only

requires:
  auth_level: admin

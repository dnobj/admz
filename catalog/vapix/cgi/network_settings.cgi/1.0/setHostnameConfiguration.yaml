id: network_settings.cgi:setHostnameConfiguration
cgi: network_settings.cgi

method: POST
min_api_version: "1.0"

request:
  content_type: application/json
  body:
    apiVersion: "1.0"
    method: setHostnameConfiguration
    params:
      hostname: "{hostname}"

response:
  format: json
  data_path: data

risk_level: normal

rollback:
  strategy: revert-config
  read_action: getNetworkInfo

requires:
  auth_level: admin

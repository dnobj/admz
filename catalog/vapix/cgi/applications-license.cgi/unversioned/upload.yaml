id: applications-license.cgi:upload
cgi: applications-license.cgi

method: POST
request:
  query:
    action: uploadlicensekey
    package: "{package}"
  content_type: multipart/form-data

response:
  format: text
  success: "OK"

risk_level: normal

requires:
  auth_level: admin

id: com-ptz.cgi:info
cgi: com-ptz.cgi

method: GET

request:
  query:
    info: "1"
    camera: "{camera}"
  content_type: null

response:
  format: text
  description: >
    Returns PTZ capabilities and current position as key=value pairs.

risk_level: read-only

requires:
  auth_level: viewer

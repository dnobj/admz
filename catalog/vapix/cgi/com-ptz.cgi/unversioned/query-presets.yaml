id: com-ptz.cgi:query-presets
cgi: com-ptz.cgi

method: GET

request:
  query:
    query: presetposall
  content_type: null

response:
  format: text
  description: >
    Returns list of preset positions as key=value pairs.

risk_level: read-only

requires:
  auth_level: viewer

id: pencil.cgi:getSelectedFilter
cgi: pencil.cgi

method: POST
min_api_version: "1.0"

request:
  content_type: application/json
  body:
    apiVersion: "1.0"
    method: getSelectedFilter

response:
  format: json
  data_path: data
  fields:
    - type

risk_level: read-only

requires:
  auth_level: operator

id: pencil.cgi:getFilterCapabilities
cgi: pencil.cgi

method: POST
min_api_version: "1.0"

request:
  content_type: application/json
  body:
    apiVersion: "1.0"
    method: getFilterCapabilities

response:
  format: json
  data_path: data
  fields:
    - filters

risk_level: read-only

requires:
  auth_level: operator

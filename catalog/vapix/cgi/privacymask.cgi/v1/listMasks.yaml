id: privacymask.cgi:listMasks
cgi: privacymask.cgi

method: POST
min_api_version: "1.0"

request:
  content_type: application/json
  body:
    apiVersion: "1.0"
    method: listMasks
    params:
      channel: "{channel:int}"

response:
  format: json
  data_path: data

risk_level: read-only

requires:
  auth_level: operator

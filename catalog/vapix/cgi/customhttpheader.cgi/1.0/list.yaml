id: customhttpheader.cgi:list
cgi: customhttpheader.cgi

method: POST
min_api_version: "1.0"

request:
  content_type: application/json
  body:
    apiVersion: "1.0"
    method: list

response:
  format: json
  data_path: data

risk_level: read-only

requires:
  auth_level: admin

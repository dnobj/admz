id: networkshare-remove.cgi:remove
cgi: networkshare-remove.cgi

method: GET
request:
  query:
    schemaversion: "{schemaversion}"
    shareid: "{shareid}"
  content_type: null

response:
  format: text

risk_level: normal

requires:
  auth_level: admin

id: temperaturecontrol.cgi:start
cgi: temperaturecontrol.cgi

method: GET
request:
  query:
    action: start
    device: "{device}"
    id: "{id}"
  content_type: null

response:
  format: text
  success: "OK"

risk_level: normal

requires:
  auth_level: admin

id: temperaturecontrol.cgi:statusall
cgi: temperaturecontrol.cgi

method: GET
request:
  query:
    action: statusall
  content_type: null

response:
  format: text
  description: >
    Returns key=value pairs with sensor names, temperatures,
    heater and fan status for all devices.

risk_level: read-only

requires:
  auth_level: operator

id: firewall.cgi:confirmRules
cgi: firewall.cgi

method: POST
base_path: /config/rest/firewall/v1
path: /conf/confirmRules

request:
  content_type: application/json

response:
  format: json

risk_level: normal

requires:
  auth_level: admin

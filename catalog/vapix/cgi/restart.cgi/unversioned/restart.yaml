id: restart.cgi:restart
cgi: restart.cgi

method: GET

request:
  query: {}
  content_type: null

response:
  format: text
  expect_timeout: true
  description: >
    Device reboots after responding. Connection will be lost.

risk_level: service-affecting

requires:
  auth_level: admin

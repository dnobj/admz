# Catalog api_id -> device-reported id mapping.
# Only entries where they differ. If a catalog api_id matches
# the device-reported id exactly, no entry is needed here.
#
# Format: catalog_api_id: device_reported_id
#
# Populated from discover_device_apis run on 2026-02-19 (6 devices).

# JSON-RPC APIs where catalog added hyphens the device doesn't use
firmware-manager: fwmgr
mqtt-event: event-mqtt-bridge

# Config-REST APIs where device reports a different name
time: time-service
